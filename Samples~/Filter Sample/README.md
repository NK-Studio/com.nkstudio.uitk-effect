# Filter Sample

Open `FilterSample.unity` to view the UITK Effects sample.

The scene demonstrates multiple drop shadows, inner shadows, outline filters, and the gradient material in a UI Toolkit document.
